# 총소리·장전 소리를 진짜 파일로 바꾸기

게임에는 소리를 직접 만들어 내는 **내장 합성음**이 들어 있습니다.
`sounds/` 폴더에 **같은 이름의 `.mp3` 파일**을 넣으면, 그 파일이 대신 재생됩니다.
파일이 없으면 지금처럼 합성음이 그대로 나오니까, 하나만 바꿔도 됩니다.

## 파일 이름

| 파일 이름 | 언제 나는 소리 |
|---|---|
| `sounds/gunShot.mp3` | 한 발 쏠 때 |
| `sounds/gunDry.mp3` | 탄창이 비었는데 방아쇠를 당길 때 (찰칵) |
| `sounds/magRelease.mp3` | 장전 시작 — 탄창 멈치 누르는 딸깍 |
| `sounds/magOut.mp3` | 빈 탄창을 뽑을 때 |
| `sounds/magIn.mp3` | 새 탄창을 끼울 때 (탁!) |
| `sounds/boltPull.mp3` | 노리쇠를 뒤로 당길 때 |
| `sounds/boltRelease.mp3` | 노리쇠를 놓을 때 (철컹) |
| `sounds/shell.mp3` | 탄피가 바닥에 떨어질 때 |
| `sounds/gunCock.mp3` | 총을 처음 얻었을 때 |

이름을 **정확히** 맞춰야 합니다. `gunshot.mp3`(소문자 s)처럼 쓰면 못 찾아요.

## 넣는 방법

1. 아래 사이트에서 소리를 내려받는다 (`.mp3` 로 저장)
2. `sounds/` 폴더에 위 표의 이름으로 넣는다
3. 커밋하고 push → Render 가 자동으로 다시 올려 줍니다

```bash
git add sounds && git commit -m "총소리 파일 추가" && git push
```

## 무료로 쓸 수 있는 소리 사이트

**남이 만든 게임이나 영화의 소리를 그대로 가져다 쓰면 안 됩니다.**
유튜브에 올라와 있어도 마찬가지예요 — 올린 사람이 만든 게 아니라 원래 게임 회사 것이니까요.
아래 사이트의 소리는 **무료로 써도 된다고 허락된 것들**입니다.

| 사이트 | 주의할 점 |
|---|---|
| **freesound.org** | 검색 후 왼쪽에서 라이선스를 **CC0** 로 걸러 쓰세요. 회원가입 필요 |
| **pixabay.com/sound-effects** | 전부 무료, 가입 없이 받을 수 있음. 가장 쉬움 |
| **opengameart.org** | 게임용 소리 모음. **CC0** 표시된 것을 고르세요 |
| **kenney.nl/assets** | 전부 CC0. 게임 소리 묶음을 통째로 받을 수 있음 |

검색어는 `gun shot`, `rifle`, `reload`, `magazine`, `bolt`, `shell drop` 으로 해 보세요.

**CC0** = 아무 조건 없이 마음대로 사용 가능
**CC-BY** = 써도 되지만 만든 사람 이름을 어딘가에 적어야 함 (게임 시작 화면 등)

## 소리 크기 조절

파일이 너무 크거나 작으면 `index.html` 의 `SOUNDS` 표에서 `vol` 값을 고치세요.
`0.55` 처럼 적혀 있는 숫자이고, **0 에 가까울수록 작게** 납니다.

```js
gunShot: { url:'sounds/gunShot.mp3', vol:0.55 },
```
