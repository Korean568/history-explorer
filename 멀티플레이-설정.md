# 👥 멀티플레이 설정 안내

## 무엇이 필요한가요?

게임 파일(`index.html`)은 그대로 두고, **작은 Node 서버 하나만** 어딘가에 올리면 됩니다.
서버 코드는 `server/` 폴더에 들어 있고 데이터베이스는 필요 없습니다.

---

## 방법 1 — Render 한 곳에만 올리기 (가장 간단)

서버가 **게임 파일도 함께 나눠 주기 때문에** 여기 한 곳만 배포하면 끝입니다.

1. 이 폴더를 GitHub 저장소에 올립니다.
   ```bash
   git init
   git add .
   git commit -m "역사탐험대 : 멀티플레이"
   git branch -M main
   git remote add origin https://github.com/<내계정>/<저장소이름>.git
   git push -u origin main
   ```

2. Render 대시보드 → **New +** → **Blueprint** → 방금 만든 저장소 선택
   - 저장소 안의 `render.yaml` 을 읽어 자동으로 설정됩니다.
   - (Blueprint 대신 **New + → Web Service** 로 만들 경우)
     | 항목 | 값 |
     |---|---|
     | Root Directory | `server` |
     | Build Command | `npm install` |
     | Start Command | `npm start` |
     | Instance Type | Free |

3. 배포가 끝나면 `https://<이름>.onrender.com` 주소가 나옵니다.
   **그 주소로 접속하면 게임이 바로 열리고, 멀티플레이도 그대로 동작합니다.**
   (서버 주소를 따로 입력할 필요가 없습니다)

> 💡 Render 무료 플랜은 15분 동안 아무도 접속하지 않으면 잠들었다가,
> 다음 접속 때 30초쯤 걸려 다시 깨어납니다. 수업 시작 전에 한 번 열어 두세요.

---

## 방법 2 — 게임은 Netlify, 서버만 Render

지금 쓰시는 Netlify 주소(`sunsasidae-salanamgi.netlify.app`)를 그대로 두고 싶다면:

1. 위와 같이 Render 에 서버를 배포합니다.
2. Netlify 에는 `index.html` 을 그대로 올립니다.
3. 게임 시작 화면 → **⚙️ 서버 주소 설정** 을 열고 아래 주소를 붙여 넣습니다.
   ```
   wss://<이름>.onrender.com/ws
   ```
   한 번 넣으면 그 기기에 저장됩니다.

   주소를 링크로 미리 넣어 나눠 줄 수도 있습니다:
   ```
   https://sunsasidae-salanamgi.netlify.app/?server=wss://<이름>.onrender.com/ws
   ```

> ⚠️ **Vercel 에는 이 서버를 올릴 수 없습니다.** 서버리스 함수라 WebSocket 연결을
> 계속 유지하지 못합니다. Neon 도 데이터베이스라 실시간 게임 동기화에는 맞지 않습니다.

---

## 방법 3 — 교실 안에서만 (인터넷 없이)

선생님 노트북에서:

```bash
cd server
npm install
npm start
```

노트북의 내부 IP(예: `192.168.0.10`)를 확인한 뒤, 태블릿에서
`http://192.168.0.10:8080` 으로 접속하면 됩니다.

---

## 게임에서 하는 방법

| | 순서 |
|---|---|
| **방장** | 이름 입력 → **🏕️ 새 방 만들기** → 나온 **4자리 코드**를 친구에게 알려 줌 → **👥 함께 탐험 시작** |
| **친구** | 이름 입력 → 코드 입력 → **참가** → **👥 함께 탐험 시작** |

- 최대 **12명**까지 한 방에 들어갈 수 있습니다.
- 화면 위쪽에 방 코드와 참가자 이름이 보입니다. 👑 표시가 방장입니다.

### 무엇을 함께 쓰나요?

| 함께 쓰는 것 | 각자 따로 쓰는 것 |
|---|---|
| 진행 순서(미션) | 🎒 아이템 (뗀석기 · 간석기 · 동검) |
| 장작 · 모닥불 | 🍖 배고픔 |
| 나뭇가지 · 가죽 · 씨앗 · 흙 · 풀 · 곡식 · 구리 · 주석 · 받침돌 · 덮개돌 | 🍎 식량 |
| 낮과 밤 · 멧돼지 | |

- 누가 나무를 베면 **모두의 장작**이 올라갑니다.
- 미니게임(뗀석기 · 간석기 · 토기 · 채굴 · 청동기)은 **각자 직접** 해야 도구를 얻지만,
  단계 완료는 방 전체에 함께 기록됩니다.
- 멧돼지에게 잡히면 **잡힌 사람만 3초 뒤 마을에서 다시 시작**하고,
  친구들의 탐험은 그대로 이어집니다.
- 다음 시대로 넘어가는 것은 **방장**이 결정합니다.

---

## 문제가 생기면

| 증상 | 확인할 것 |
|---|---|
| "서버에 연결할 수 없어요" | ⚙️ 서버 주소 설정에 `wss://...onrender.com/ws` 를 넣었는지 |
| "그런 방이 없어요" | 코드 4자리를 정확히 입력했는지 (대소문자 구분 없음) |
| 첫 접속이 아주 느림 | Render 무료 플랜이 잠들어 있던 것 — 30초쯤 기다리면 깨어납니다 |
| 방장이 나감 | 남은 사람 중 한 명이 자동으로 방장이 됩니다 |
| 서버 상태 확인 | `https://<이름>.onrender.com/health` → `ok`, `/rooms` → 현재 방 목록 |
